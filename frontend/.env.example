# Copy to .env
# Must be prefixed with PUBLIC_ for Astro to expose it to client-side islands
# (see WaitlistForm.astro). Server-side code (frontmatter, at build time) can
# also read any non-PUBLIC_ var directly via import.meta.env.
PUBLIC_API_URL=http://localhost:8000
